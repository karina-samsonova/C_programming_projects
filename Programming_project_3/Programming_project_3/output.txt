#define _CRT_SECURE_NO_WARNINGS
#include < stdio.h > 
int a () 
{
	return b (); 
} 
int b () {
	return c (); 
} 
int c () {
	return 0; 
} 
int main () 
{
	for (;;) {
	} 
	int abc; 
	int x = 0; 
	while (a < 10 && (b == 0 || c < 3) ) {
		//a++;
		//b++;
		while (a < 10 && ( b == 0 || c < 3) ) {
			a++; 
			b += 1; 
			++c; 
		} 
		c--; 
	} //ком
	//мен
	//тарий
	double y; 
	return 0; 
} 



17 строка> Переменная 'abc' не получила начального значения.
31 строка> Переменная 'y' не получила начального значения.
Максимальная вложенность циклов равна 2.
В программе нет рекурсивных функций.
14 строка> Оператор 'for (;;)' может стать причиной зацикливания.
19 строка> Оператор 'while (a < 10 && (b == 0 || c < 3) )' может стать причиной зацикливания.
